<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:PresentationControls="clr-namespace:Benday.Presentation.Controls;assembly=Benday.Presentation"
             xmlns:ViewModels="clr-namespace:MauiApp1.ViewModels"
             x:Class="MauiApp1.MainPage">
    <ContentPage.BindingContext>
        <ViewModels:Test123ViewModel></ViewModels:Test123ViewModel>
    </ContentPage.BindingContext>

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a race car number eight" />

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Button
                x:Name="CounterBtn"
                Text="Click me" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />

            <Entry
                x:Name="CounterEntry"
                Text="{Binding Counter}"
                IsReadOnly="True"
                HorizontalOptions="Fill"
                Completed="CounterEntry_Completed"
                SemanticProperties.Description="Displays the number of times you clicked the button" />

            <PresentationControls:TextboxField LabelText="String Field" BindingContext="{Binding StringField}" IsVisible="true"></PresentationControls:TextboxField>
            <PresentationControls:TextboxField LabelText="Int Field" BindingContext="{Binding IntField}" IsVisible="true"></PresentationControls:TextboxField>

            <Button Text="Debug" Clicked="Button_Clicked"></Button>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
